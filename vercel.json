{
  "buildCommand": "curl -o- https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.5-stable.tar.xz | tar -xJf - && git config --global --add safe.directory /vercel/path0/flutter && export PATH=\"$PATH:$PWD/flutter/bin\" && flutter build web --release --base-href /",
  "outputDirectory": "build/web",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}